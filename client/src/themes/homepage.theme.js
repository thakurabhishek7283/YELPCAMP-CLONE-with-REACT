import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#c64516",
      light: "#ff7543",
      dark: "#8e0c00",
    },
    secondary: {
      main: "#006750",
      light: "#41967c",
      dark: "#003b28",
    },
  },
});

export default theme;
